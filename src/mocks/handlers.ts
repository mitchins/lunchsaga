import { http, HttpResponse } from 'msw'

export const handlers = [
  // Add your API handlers here as needed
  // Example:
  // http.get('/api/teams', () => {
  //   return HttpResponse.json({ teams: [] })
  // }),
]
